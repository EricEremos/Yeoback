# Yeoback mobile preview

Make room, deliberately. The mobile preview finds explainable file candidates in a folder you choose, then separates selection, review, deletion and observed outcomes.

This is a development preview. It does not scan other apps' private storage, prove that a file was generated by AI, determine project usage, or guarantee a free-space reserve. The installed Mac app remains separate.

## Native targets

| | iPhone / iPad | Android |
|---|---|---|
| Interface | SwiftUI, semantic system fonts | Kotlin, Compose Material 3 |
| Minimum OS | iOS 17 | Android 10 / API 29 |
| Access | System directory picker, security-scoped URL | System folder picker, persisted SAF grant |
| Metadata | File identity, size, modification/change timestamps | Provider document ID, name, size, modification timestamp |
| Cleanup | Coordinated file removal after revalidation | Provider deletion after metadata, grant and containment checks |
| Recovery | No Yeoback Trash or Undo promised | No Yeoback Trash or Undo promised |

Both targets use the Yeoback paper, ink and coral palette in Light and Dark. Classification uses filename and folder clues for SVGs, drafts/exports and work records. These are review categories, not safe-to-delete labels.

## Build and verification

The [mobile workflow](https://github.com/EricEremos/Yeoback/actions/workflows/mobile.yml) is configured to build native targets and run disposable-fixture tests. Artifacts expire after seven days. A green workflow proves its tested fixtures; real iCloud, third-party document providers and physical devices require separate verification.

For iOS, open `ios/Yeoback.xcodeproj` in Xcode with the iOS SDK installed. The checked-in project can be regenerated with `python3 ios/make-project.py`; `python3 ios/make-assets.py` regenerates named color assets from the brand palette. CI builds without code signing for an iPhone simulator. Physical-device signing is deliberately not configured with an invented Apple team.

For Android, see [native build instructions](android/README.md). The project pins its Gradle distribution and verifies its checksum. A JDK 17 and Android SDK 35 are required; CI supplies them without installing SDKs on the user's Mac.

The iOS file engine also has a local macOS fixture smoke test, `zsh scripts/check-mobile-core.sh` from the repository root. This exercises real temporary-file scanning and removal but does not replace iOS permission or UI testing.

## Design and evidence

- [Editable mobile flow in the existing Figma file](https://www.figma.com/design/olCrS1PxKWJjMiVlGbC3zR?node-id=233-465)
- [Platform research and scope](../docs/YEOBACK-MOBILE-SCOPE.md)
- [Exact Figma node ledger](../docs/figma-mobile-state.json)

The Figma frames document structure and semantic states. Native controls follow their platform's rendering and accessibility behavior; these frames are not claims of pixel-identical implementation or participant validation.
